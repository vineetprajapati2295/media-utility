version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=False
      - PORT=5000
    restart: unless-stopped

